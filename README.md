# ELF Exercises

This repo contains exercises in manually writing ELF files for amd64/Linux using
only `elf.h` and only being provided the encoded instructions of a program's
text section to execute. Each directory contains an exercise starting with a
basic statically linked C program working up towards more advanced C
functionality.
